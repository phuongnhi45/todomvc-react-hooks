import React from "react"
import PropTypes from 'prop-types'

const TodoHandle = ({deleteId}) => {
  
  

  return (
    <div></div>
  );
}
TodoHandle.protoTypes = {
  deleteId: PropTypes.func
}
export default TodoHandle